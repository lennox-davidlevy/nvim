potential way to deal with server configs in nvim-lsp with mason-lspconfig
may have to find a better way to orgranize this stuff. 
-- local server_configs = {
--   lua_ls = {
--     settings = {
--       Lua = {
--         diagnostics = {
--           globals = { "vim" }
--         }
--       }
--     }
--   }
-- }
--
-- require("mason-lspconfig").setup_handlers({
--   function(server_name)
--     require("lspconfig")[server_name].setup(server_configs[server_name] or {})
--   end
-- })
